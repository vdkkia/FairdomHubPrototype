describe("Single page flowchart", function () {
  beforeEach(function () {
    this.timeout(10000);
    MagicLamp.load("single_pages");
  });

  it.only("renders the flowchart canvas", function () {
    // expect($j("#folder_column").length).to.equal(1);
  });
});
